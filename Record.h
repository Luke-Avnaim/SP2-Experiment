#ifndef RECORD_H
#define RECORD_H
#include <string>
#include <queue>

typedef struct record {
    string input;
    string output;
} record;

typedef struct recordQueue {
    queue record;
} records;

#endif